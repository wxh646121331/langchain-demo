# 工具的组件
- 名称：str 在提供给 LLM 或 agent 的一组工具中必须是唯一的
- 描述：str 描述工具的作用。被 LLM 或 agent 用作上下文
- args_schema： pydantic.BaseModel 可选但推荐，如果使用回调处理程序则为必需。它可用于为预期参数提供更多信息或验证。
- return_direct：boolean 仅与agent相关。当为 True 时，在调用给定工具后，agent 将停止并将结果直接返回给用户。
## 1. @Tool 装饰器定义

## 2. 继承 BaseTool 定义

## 3. 从 MCP 服务器端获得

